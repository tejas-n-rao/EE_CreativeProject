{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "BenchmarkStat",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "category", "metric", "unit", "value", "citations"],
  "properties": {
    "id": { "type": "string" },
    "category": { "type": "string" },
    "metric": { "type": "string" },
    "unit": { "type": "string" },
    "value": { "type": "number" },
    "citations": {
      "type": "array",
      "items": { "$ref": "#/definitions/citation" },
      "minItems": 1
    }
  },
  "definitions": {
    "citation": {
      "type": "object",
      "required": ["title", "url", "accessed_on"],
      "additionalProperties": false,
      "properties": {
        "title": { "type": "string" },
        "url": { "type": "string", "format": "uri" },
        "accessed_on": { "type": "string", "format": "date" }
      }
    }
  }
}
